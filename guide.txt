&amp; -> Makes an Ampersand
<span> </span> -> Group and mark up a specific part of a text or document.
Ex: <p>The weather today is <span style="color: red; font-weight: bold;">extremely hot</span> out there.</p>

The code <html lang="en"> Specifies that the primary language of the webpage is English
aria-current="page": tells that this link represents the current page in the navigation structure.
&mdash; : Makes an em dash
&middot; : middle dot symbol (·)
 <p class="pill">Student &middot; Writer &middot; Maker</p> for a pill like box
 &rsquo; : apostrophe
class="acheivement"
<label>: This tag defines a caption for an item in a user interface.
for="build-1": This attribute explicitly binds the label to a form element (like an <input>) that has th
the matching id. 
xyz: This is the actual text that the user will see on the screen.